const myAge = 10; // number
const myName = "Đạt"; // string
const isStatus = true; // boolean

const myChildren = ["Kiên", "Thành", "Lâm"]; // object
const running = function () {
    // object
    return "running";
};

console.log(myName);
console.log(running());

const myInfo = {
    name: "Dat",
    age: 10,
    isStatus: true,
    children: ["Kiên", "Thành", "Lâm"],
    running: function () {
        return "running";
    },
};

console.log(myInfo.name);
console.log(myInfo.running());